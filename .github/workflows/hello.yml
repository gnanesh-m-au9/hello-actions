name: Hello workflow
on: 
  push: {}
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - name: Print context
    run:
      echo "Hello Github Actions"
      echo "${{ github.ref }}"
      echo "Branch Name ID ${{ github.sha }}"
