name: Task 3 - Multi Step

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Show files
        run : las -la

      - name: Simulate build
        run: echo "Building..."