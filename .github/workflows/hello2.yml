name : Task1 - Hello CI
on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      -name : Print context
      run: | 
        echo "Hello Github Actions"
        echo "Ref: ${{ github.ref }}"
        echo "SHA: ${{ github.sha }}"