name: Docker Build

on:
  push:
    - main:

jobs:
  docker:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v7

      - name: Build Docker image
        run: docker build -y myapp . 