name: Task 7 - Job Graph

on:
  push:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lint OK"
  
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests OK"
  
  package:
    needs: [lint, test]
    runs-on : ubuntu-latest
    steps:
      - run: echo "Packaging..."